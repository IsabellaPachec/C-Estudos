#include <iostream>
using namespace std;
const int NUM_ELEM = 10;

int main()
{

    // VETOR INTEIRO
    // int c[10] = {14, 0, 13};

    // c[5] = 30;
    // c[7] = 40;
    // c[8] = 50;

    // for(int i = 0; i < 10; i++){
    //  cout << "c[" << i << "] = " << c[i] << "\n";
    //}

    // VETOR COM CONSTANTE
    // int c[NUM_ELEM];
    // for (int i = 0; i < NUM_ELEM; i++){
    // c[i] = 2*i;
    // }

    // for(int i = 0; i < NUM_ELEM; i++){
    //  cout << "c[" << i << "] = " << c[i] << "\n";
    //}

    // VETORES DINAMICOS
    // int num_elem;
    // cout <<"Digite o tamanho do vetor: ";
    // cin >> num_elem;

    // int* c = new int[num_elem];

    // for (int i = 0; i < num_elem;i++ ){
    // c[i] = 2*i;
    //}

    // for(int i = 0; i < num_elem; i++){
    // cout << "c[" << i << "] = " << c[i] << "\n";
    //}

    

    return 0;
}